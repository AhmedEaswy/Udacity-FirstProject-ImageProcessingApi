interface Options {
  root: string
}

interface FileOptions {
  fullPath: string
  convertPath: string
  h: any
  w: any
  fileName: string,
  covertedFileName: string
}

export { Options, FileOptions }
